# Project Rules

> [!IMPORTANT]
> **ESTAS REGLAS NO SON NEGOCIABLES.**
> SIEMPRE se deben seguir las reglas establecidas en este documento. 
> JAMÁS se deben omitir, bajo ninguna circunstancia.


## Versioning Policy
- **Always** increment the patch version in `package.json` (e.g., `3.3.23` -> `3.3.24`) after making functional changes or fixes.
- This ensures that every build has a unique version number for tracking and updates.
